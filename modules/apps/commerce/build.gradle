apply plugin: "com.liferay.app.defaults.plugin"

subprojects {
	configurations.all {
		resolutionStrategy {
			force group: "com.liferay", name: "com.liferay.portal.tools.rest.builder", version: "1.0.22"
		}
	}
}