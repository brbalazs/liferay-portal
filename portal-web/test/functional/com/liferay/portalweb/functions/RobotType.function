@default = "robotTypeShortcut"
@summary = "Type to '${locator1}' the value '${value1}'"
definition {

	function robotTypeShortcut(locator1) {
		WaitForSPARefresh();

		selenium.robotTypeShortcut(${locator1});
	}

}