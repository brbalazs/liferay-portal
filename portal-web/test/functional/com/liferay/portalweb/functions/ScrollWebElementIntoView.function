@default = "scrollWebElementIntoView"
definition {

	function scrollWebElementIntoView(locator1) {
		WaitForSPARefresh();

		selenium.scrollWebElementIntoView(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}